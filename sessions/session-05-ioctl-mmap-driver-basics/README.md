# Session 05：`ioctl`、`mmap` 与驱动基础

## 目标

建立用户态到内核态的基本调用视角，理解 `ioctl` 和 `mmap` 在驱动中的角色。

## 核心问题

- 为什么图形驱动高度依赖 `ioctl`？
- `mmap` 如何把设备内存或对象映射到用户态？

## 建议输出

- 用户态到内核态调用链笔记
- `ioctl` / `mmap` 速记

## 关联 Lab

- [Lab 05：`ioctl`、`mmap` 与驱动基础](../../labs/lab-05-ioctl-mmap-driver-basics/README.md)
